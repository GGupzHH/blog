// 添加额外的样式

// 全站变灰(哀悼日)
html, body
  font-size 14px
  .sidebar 
    font-size 14px
//   filter grayscale(100%)
//   -webkit-filter grayscale(100%)
//   -moz-filter grayscale(100%)
//   -ms-filter grayscale(100%)
//   -o-filter grayscale(100%)

.theme-container
  // 头部 navbar
  .navbar {
    font-sans-serif: fa-proxima-soft, "Helvetica Neue", Helvetica, Arial, sans-serif;
    opacity: 0.88;
    background-color $navBgColor
    border-bottom-color $navBgColor
    box-shadow: var(--box-shadow)
    /* 渐变色标题 */
    .site-name {
      color $accentColor
    }
    .logo {
      border-radius 50%  
    }
    .search-box input:focus {
      border-color $textColor
    }
    .links {
      background-color $navBgColor
      // color #ecf0f1
    }
    .nav-links a:hover, .nav-links a.router-link-active {
      color $accentColor
    }
    .nav-item > a:not(.external):hover, .nav-item > a:not(.external).router-link-active {
      border-bottom: 2px solid #fff;
    }
    .arrow.down {
      color: #fff;
      border-top: 6px solid #fff;
    }
    .reco-github {
      color: var(--text-color);
    }
    .icon.outbound {
      color: #fff;
    }
  }
  // ???
  // .sidebar-mask {}
  // 侧边栏
  .sidebar {
    /*定义滚动条高宽及背景 高宽分别对应横竖滚动条的尺寸*/
    &::-webkit-scrollbar {
      width: 0;
      height: 0;
    }
    &:hover {
      &::-webkit-scrollbar {
        width: 6px;
        height: 6px;
        background-color: #ecf0f1;
      }
      /*定义滚动条轨道 内阴影+圆角*/
      &::-webkit-scrollbar-track {
        background-color: #ecf0f1;
      }
      /*定义滑块 内阴影+圆角*/
      &::-webkit-scrollbar-thumb {
        border-radius: 2px;
        background: #95a5a6;
      }  
    }
  }
  // 首页
  .home-blog {
    .hero {
      align-items: flex-end;
      margin: 0 auto 0;
      h1 {
        display: none;
      }
      .description {
        font-size: 3.2rem;
        color: #12759e;
      }
    }
    .hero img {
      
    }
  }
  // 内容页
  .page {
    position relative
    h2 {
      border none  
    }
    .custom-block {
      &.tip {
        border-color: $badgeTipColor;
      }
      &.warning {
        border-color: $badgeWarningColor; 
      }
      &.danger {
        border-color: $badgeErrorColor;  
      }
    }
  } 
  

